vim.diagnostic.config({
    virtual_text = false,
    -- virtual_lines = {
    --     only_current_line = true
    -- }
})


-- vim.keymap.set(
--     "",
--     "<Leader>ll",
--     require("lsp_lines").toggle,
--     { desc = "Toggle lsp_lines" }
-- )
